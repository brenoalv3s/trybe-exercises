let costProduct = 10;
let saleValue = 12;
let tax = (constProduct * 0.2) + constProduct;

if (constProduct > 0 && saleValue > 0) {
    let total = ((saleValue - tax) * 1000);
    console.log("Valor do lucro é: " + total);

} else {
    console.log ("Valores inválidos"); 
};